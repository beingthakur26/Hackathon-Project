services:
  - type: web
    name: toxinai-backend
    env: docker
    dockerContext: .
    dockerfilePath: backend/Dockerfile
    plan: starter
    healthCheckPath: /
    envVars:
      - key: MONGODB_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 8000
